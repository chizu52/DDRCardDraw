.pnp.*
.yarn/*
.partykit/*
.env
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

dist
node_modules
/*.zip
scripts/scraping/textage/**
scripts/scraping/sanbai/**

# User-supplied fonts for OBS overlays (see
# src/other-assets/fonts/README.md) -- kept out of git since font
# licenses typically permit bundling into an app but not redistributing
# the raw file, and this repo is public. Optional: overlays fall back
# to a system font stack when these are absent.
src/other-assets/fonts/title-font.*
src/other-assets/fonts/body-font.*
